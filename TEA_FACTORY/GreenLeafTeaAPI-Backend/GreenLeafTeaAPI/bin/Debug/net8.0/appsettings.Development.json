{
  // ============================================================
  // appsettings.Development.json
  // Overrides settings when running in Development mode.
  // This file is NOT used in production (safe to have dev-only
  // settings here like verbose logging).
  // ============================================================
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
